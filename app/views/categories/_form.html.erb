<%= form_for @category do |f| %>
    <div class= "form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %> <br>
    </div>

    <%= f.submit @category.persisted? ? "Update Category" : "Add Category" %>
<% end %>